language: bash

sudo: required

services:
  - docker

before_install:
  - docker image build -t maheshambiga/profile-service-test -f ./profile/Dockerfile.dev ./profile
  - docker image build -t maheshambiga/auth-service-test -f ./auth/Dockerfile.dev ./auth

script:
  - docker run maheshambiga/profile-service-test npm test -- --coverage
  - docker run maheshambiga/auth-service-test npm test -- --coverage

after_success:
  - docker build -t maheshambiga/profile-service ./profile
  - docker build -t maheshambiga/auth-service ./auth
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push maheshambiga/profile-service
  - docker push maheshambiga/auth-service
